
## Cycle 1 (2026-07-18, autonomous)
- Water & Proportion tab: spliced opus "where the water actually goes" ranked infographic (thermoelectric 133 / irrigation 118 Bgal/d area-squares + ranked bars + AI "off the national chart" + local-scarcity honest note). apply_wpx.sql. VERIFIED live, theme-native, no overflow, zero words lost.
- In flight: per-query batching-lever viz (a3fc1d99281d61cb7), narrative one-study-many-countries fan (a4a265db4635fe46b).
- Backups: env_bak_water.html, water_block.html.
- Per-query tab: spliced opus "batching is the lever" GPU-die visual (empty red 1-user die ~3% util 2-10Wh vs full teal batched die ~90% 0.05-0.3Wh, 10-50× lever, formula w/ concurrent-queries highlighted). apply_blx.sql. VERIFIED live, no overflow, zero words lost. Cycle 1 COMPLETE.

## Cycle 2 (2026-07-18)
- Narrative tab: spliced opus "one study → three countries" fan (de Vries 85-134 TWh source node → SVG fan connectors → 🇳🇱BBC/🇸🇪DCD/🇦🇷EnergyCentral cards + 🇯🇵Japan/🇮🇪Ireland separated-sources aside). apply_telx.sql. VERIFIED, no overflow, zero words lost.
- Env page now richly illustrated across ALL tabs. Transitioning to: 1-2 more signature vizzes on other pages (moral-patienthood asymmetry matrix) + FACT-CHECK phase (re-verify all load-bearing numbers, adversarially, a few times).
- Backups: env_bak_narr.html, narr_block.html.

## Cycle 3 (2026-07-18)
- Moral-patienthood: replaced plain text-grid matrix with opus .mpx asymmetry infographic (2×2, Catastrophe/S-risk cell red-dominant w/ full impact meter, merged old vivid phrasings "suffering multiplied by instance count..."/"embarrassing recoverable" so no words lost). apply_mpx.sql. VERIFIED, no overflow.
- FACT-CHECK BATCH 1 (7 claims): 6 CONFIRMED, 1 DISCREPANCY FIXED — page said ">3× Google compute/energy in 5yr" but Google's actual figure is >6× ("six times more computing power per unit of electricity"; the 3× is a separate TPU carbon-efficiency metric). Fixed all 4 instances (stat card, area square resized √6, non-replacement callout, source line) → >6×. apply_6x.sql.
- Backups: moral_bak.html, mpx_block.html, env_bak_6x.html.

## Fact-check pass 1 complete (2026-07-18) — 24 claims, 2 fixes
- Batch 1 (energy/operator): 6/7 ✓, FIXED >3×→>6× (Google compute/electricity in 5yr).
- Batch 2 (aggregate/forecast/crypto): 6/7 ✓, FIXED 1,200→1,100 TWh (2024 global electricity DEMAND growth +4.3%; 1,200 was generation). apply_1100.sql.
- Batch 3 (water/framing/supply-chain): 10/10 ✓ (FAO 69/19/12, USGS 133/118, Li 500ml, Mistral 45ml, Strubell 626155/1438/126000, Patterson 88×/18.7×, capex 226/410/725, CoWoS 90%, B200 208B, streaming 18g/90×).
- ALL other flagship numbers CONFIRMED verbatim vs primaries (IEA PDF, Goldman, de Vries Joule, Epoch, Google, Luccioni, Splitwise, DeepSeek). KB is solid.
- Backups: env_bak_1100.html.

## Fact-check pass 2 complete (2026-07-18) — 10 items, 0 errors
- Derived figures ALL correct: AI 0.2% (1.5%×~14-15%), 1,850 Mt (=5% of verified 37.8 Gt global energy CO2), 600× (18g video ÷ 0.03g prompt exact), 170× water (45÷0.26=173). Flagship Epoch 0.3Wh / Google 0.24Wh-0.26ml / IEA 1.5%-415TWh triangulated independently. Local Action $64B / JLARC $14-37/mo / AEP Ohio 85%/12yr all CONFIRMED.
- Nuances noted: Epoch 0.3Wh is central-but-low-end (indep benchmark arxiv 2505.09598 ~0.42Wh; Altman ~0.34); Google 0.24/0.26 numbers sound but SCOPE critiqued (median hides heavy prompts, water=cooling-only not generation, market-based carbon, not peer-reviewed) → STRENGTHENED §05 caveat with this critique (apply_gcrit.sql). Data Center Watch is 10a-Labs-funded + $46B of $64B is "delayed" not blocked (already noted on Local Action page).
- VERDICT: 34 verifications across 2 passes, 2 minor fixes, KB is solid & honestly-scoped.

## Light-cadence wakeup (2026-07-18): health + final fact-check pass 3
- All 13 pages HTTP 200 (health confirmed).
- Dispatched final adversarial spot-check (a3df66c272515d02d): least-verified anchor claims — DeepMind cooling 40%/15%PUE/30%, GenCast 8min/1TPU, BrainBox 15.8% HVAC, Microsoft closed-loop 125M L/yr, Luccioni 950-smartphone-charges; + Grok/xAI CSAM entry regulatory facts (CA AG Bonta, UK Ofcom, House E&C letter allegations 23k/1.8M/7751 — confirm framed as REPORTED not proven).
- Plan: apply any discrepancy fixes → then STOP the loop (KB will have had 3 verification passes over ~40 numbers).

## Fact-check pass 3 complete + LOOP STOPPED (2026-07-18)
- Net-benefit claims ALL CONFIRMED: DeepMind cooling 40%=15%PUE + autonomous ~30%; GenCast 8min/1 TPU v5; BrainBox 15.8% HVAC/234821kWh/37tCO2e @45 Broadway; Microsoft zero-water closed-loop >125M L/yr + WUE 0.49→0.30.
- Grok/xAI CSAM entry: CA AG Bonta (Jan 14 2026) + UK Ofcom (Jan 12 2026, £18M/10%) + House E&C letter allegations (23k children/1.8M women/7751-per-hr) ALL CONFIRMED + correctly framed as REPORTED. Added precision: figures are from an analyzed sample of ~4.4M images (not a census) + "at least 1.8M" → updated real_issues desc (-U campus) + rebuilt atlas (apply_atlasg.sql).
- Luccioni "950 smartphone charges" discrepancy (paper v3 says 522) = NOT on our page, no fix needed.
- FINAL TALLY: 3 adversarial fact-check passes, ~42 verifications, 3 fixes total (>3×→>6×, 1200→1100 TWh, Grok 4.4M-sample precision). KB thoroughly verified & honestly-scoped. LOOP STOPPED — pages in excellent shape, avoiding over-decoration.

## Agriculture-pushback rebuttal (2026-07-18, post-loop, direct request)
- Added §08 callout answering "agriculture feeds living things so its water doesn't count!" — concedes moral force (food > chatbot, scale/leverage argument not a value ranking) then rebuts on specifics: #1 US user thermoelectric cooling (133 Bgal/d) feeds nothing; Colorado Basin cattle-feed crops (alfalfa/hay) ~46-55% of water / ~2/3 of ag use [VERIFIED LA Times/Richter et al + E&E]; beef ~1,800 gal/lb [FoodPrint]; alfalfa exported = discretionary animal feed. Upgraded alfalfa ESTIMATE tag → VERIFIED w/ the 46-55% Colorado figure. +2 sources. apply_ag.sql. VERIFIED live, no 360 overflow.

## Theme/tabs/content fixes (2026-07-18, direct requests, verified on galactic-map theme)
1. READABILITY: fancy themes (galactic-map: wireframe cube + starfield, --color-bg #030509) bled through content. Fix: gave `.env2` root `background:var(--color-bg,#0c0d10)` → solid reading surface, decoration now only in margins. Verified legible.
2. TABS obvious: redesigned tab bar — labeled ("THIS PAGE IS IN SECTIONS — TAP ONE TO READ IT · SWIPE FOR ALL 7 →"), solid bordered container (occludes decoration), clear pill buttons w/ borders + filled active, right-edge scroll fade. Was: transparent border-only strip, not obviously clickable, truncated.
3. REMOVED leaked subagent commentary: "The visual verifies cleanly at all three widths — 900px wrapper... Here is the block." had spliced into §01 (per-query extraction grabbed the agent's preamble). Removed. Audited all spliced blocks — only 1 leak.
4. LOCAL MODELS added to §06: 7B/32B run on laptop/small GPU, 70B on high-mem ARM (Apple Silicon via MLX, or Ampere/Graviton) or modest server; FP8 (8-bit) ~halves memory (70B: 140GB→70GB) = less energy+silicon/token → inference leaving the data-center picture.
5. ACRONYM GLOSSARY: collapsible <details> after lede (accessible from any tab), 18 terms (Wh/kWh/TWh, GW, PUE, WUE, TDP, GPU/TPU, MoE, HBM, CoWoS, FP8/FP16/BF16, LCA, LLM, ARM/MLX, NAS, CO2e, NCII/CSAM, IEA/FAO/USGS, JLARC/PUCO).
All verified: galactic-map theme readable, tabs obvious, no 360px overflow, glossary→1col mobile. apply_fix1/fix2.sql.

## Cross-navigation: reach every page from every page (2026-07-18, direct request)
PROBLEM: `ai-problems-index` is an SPA shell that fetch-loads the 12 topic pages into tabs — so the hub reaches all. But the 12 sub-pages, visited DIRECTLY at /x/<slug>, had no nav back: only an inconsistent footer ("Part of the AI Problems Index · see Risk Atlas and Environmental Impact") on ~7 of them; atlas/env/research/security-map had nothing.
FIX: one shared, self-contained top nav strip (`nav.xnav`) prepended to all 12 topic pages — prominent "AI Problems Index" home button + "JUMP TO" + 12 sibling chips (with icons). Now every page reaches every other in 1 hop, plus the hub.
- Self-contained: carries its own scoped <style> + tiny <script> (marks active chip via location.pathname, drops its href).
- Theme-safe: solid `--color-bg-card` surface (readable on galactic-map), chips horizontally scrollable, wraps at ≤640px.
- Embed-hide: `.apx .xnav{display:none!important}` — when the SPA injects a page's innerHTML, the strip hides so the hub's own tab bar isn't duplicated. Verified: injected env page has 1 xnav, 0 visible inside SPA.
- Published via guarded SQL (apply_xnav.sql): `WHERE slug IN (12) AND content_html NOT LIKE '%class="xnav"%'` — idempotent. All 12 → has_nav=1.
- Local canonicals re-synced from post-publish DB so a future full republish keeps the nav: env7_content.html, moral_content.html, atlas_content.html, research_content.html, dca_content_v2.html (dca DB was ahead of local — peak-note reworded — so refreshed from DB, not overwritten).
SCOPE: the 12 pages the index tabs cover. Deliberately excludes cognition, enterprise-ai, AgenticIndustrialAutomation, and agentic-security sub-library pages (only -map is in the index). One-line chip-list addition if Liz wants them.
VERIFIED (Playwright): galactic-map desktop 1200px (solid nav, active=Environment, no overflow), SPA embed-hide (0 visible xnav in tab), 360px mobile atlas (nav wraps, chips scroll, active=Risk Atlas, docScrollW 349<360). Block: nav_block.html.

## Front page → Threat Atlas scoreboard (2026-07-18, direct request)
COMPLAINT (Liz): the index's headline numbers celebrated how much work I did (53 entries, 280 sources, 98 papers, 12 deep-dives) — she wants a scoreboard: "how good are we doing on these AI risk scenarios." Plus mid-turn: atlas "just says ai env footprint is ongoing" (a type tag, no how-are-we-doing), and "should be owned by school", and "what happened to the db" (answered: real_issues intact, 53 rows).
DECISIONS (asked Liz): axis = STATE + TREND arrow; tone = graded WITH confidence tags.
BUILT:
- New DB table `risk_scoreboard` (SCHOOL-owned, per Liz) — 15 curated scenarios grouping the 53 real_issues entries. Cols: ord,name,state,trend,conf,go_tab,why. Source of truth in DB (create_scoreboard.sql).
- state: contained|holding|slipping|losing|unknown (green→red→grey). trend: up|flat|down|q. conf: measured|estimated|contested (measured reserved for genuinely-counted facts — only Energy).
- gen_scoreboard.py: reads risk_scoreboard via psql json_agg, injects SCORE literal between /*SCOREBOARD-START/END*/ markers in index_live.html. Edit DB → re-run gen → publish. DO NOT hand-edit the array.
- index page reworked: hero now "How are we doing on AI's real risks?"; KPI vanity row REPLACED by the scoreboard (tally pills + 15 rows w/ colored state bar, why subtitle, verdict); corpus numbers demoted to one foot line. Rows click → deep-link to the relevant SPA tab.
TALLY (current call): 4 losing · 5 slipping · 4 holding · 1 contained · 1 unknown. Trend: 10↓ 2→ 2↑ 1?.
ADVISOR FIXES applied: (1) Energy recalibrated holding↓ (was slipping↓) to match env page's deflationary thesis — no whiplash. (2) CSAM softened to "reported", conf estimated (respects reported/alleged constraint). (3) Grok "MechaHitler" got its own row (Owner-controlled model steering) — was a Critical entry with no home. (4) trimmed reflexive `measured` to estimated/contested on judgment calls; added AI-risk denialism holding↑ + jobs contained for honest variance.
VERIFIED (Playwright, galactic-map): 15 rows render, tally correct, KPIs gone, hero reframed, no 1200px or 360px overflow, row-click → correct tab (Energy→#env), mobile verdict wraps cleanly.
FOLLOW-UP (not yet done): propagate per-entry state/trend/conf into real_issues (53 rows) so the ATLAS entries also show "how are we doing" instead of just Critical/Emerging/Ongoing — Liz's env-footprint note. Needs 3 new columns + atlas render update + 53 editorial calls. Awaiting her go + veto pass on the 15 scenario grades first.

## Atlas → per-entry SCORECARD: all 53 real_issues graded (2026-07-18, direct request)
Liz: "do the 53 real issues, that's what I meant as the scorecard" (the atlas "just says env footprint is ongoing" — type tag, no how-are-we-doing). Made the ATLAS the scorecard.
DB: added 4 cols to `real_issues` (campus-owned; ALTER as -U campus) — score_state, score_trend, score_conf, score_note. Graded all 53 (apply_scorecard.sql, grades in scorecard_grades.json). Same vocab as hub: state contained|holding|slipping|losing|unknown, trend up|flat|down|q, conf measured|estimated|contested.
TALLY: 5 losing · 19 slipping · 23 holding (plurality, not a red wall) · 6 unknown · 0 contained. Trend 25↓ 17→ 6↑ 5?. Conf: 1 measured (energy only, disciplined) · 30 estimated · 22 contested.
BUILDER: rewrote build_atlas.py — scoreline (state+trend+conf) is now the PRIMARY badge; old Critical/Emerging/Ongoing DEMOTED to a small "type" tag (advisor: two status words = the confusion Liz flagged). Added top state-tally pills + legend distinguishing SCORE from TYPE. Filter buttons now by STATE (was by type). Cards sorted worst-first (losing→unknown). Each card's score_note shows as an "Assessment" callout on expand (colored by state). Left border color = state. FOLDED nav_block.html into builder output (build_atlas overwrites atlas_content.html from scratch → would've dropped xnav).
PIPELINE (rerun after any real_issues edit): re-dump atlas_issues.json WITH score_* cols + sources (json_agg, -U campus) → python3 build_atlas.py → publish atlas_content.html to pages 'ai-risk-atlas' as -U school (apply_atlas.sql, $atlasx$ tag).
ADVISOR fixes applied: scoreline primary + type demoted + tally + legend (render coherence); re-dumped with new cols (not stale file); nav folded in; measured reserved for the 1 counted entry; anti-skew (23 holding plurality, 6 up on research-progress front). Consistency w/ hub 15 verified (deepfakes/democracy/cyber/bio/misalignment/interp/gov/energy/CSAM/steering/surveillance/military/psych/denialism all match; socioeconomic=holding vs hub jobs=contained is deliberate scope diff, threaded in the note).
INTEGRITY: 0 original content chunks lost (title/summary/desc/why/doing/sources all survive), all 53 notes render.
VERIFIED (Playwright galactic-map): 53 cards worst-first, tally 5/19/23/6, state filter works (Losing→5), scoreline primary + CRITICAL demoted, Assessment callout on expand, sources intact, no overflow desktop+360px, and inside SPA #atlas (xnav hidden, 53 cards render).
OPEN Qs for Liz: (a) 0 of 53 are "contained" — honest read or is something genuinely contained? (b) real_issues stays campus-owned (added cols as campus); if she wanted the whole KB table school-owned that's a separate migration. (c) hub jobs=contained vs atlas socioeconomic=holding scope mismatch — flag.

## Dug into "jobs apocalypse" — regraded contained→slipping (2026-07-18, Liz: "seems like it is pretty bad")
Liz pushed back on my contained/holding grade. Researched 2025-26 labor data (Tavily + fetched the primary PDF). Finding: my grade WAS too sanguine — but the honest picture is a SPLIT, not a wave:
- **Stanford "Canaries in the Coal Mine?"** (Brynjolfsson/Chandar/Chen, Nov 13 2025, ADP payroll data — pdftotext'd the primary): early-career (22-25) in most AI-exposed occupations = **16% relative employment decline** since ChatGPT; experienced workers stable. Six facts: adjust via HEADCOUNT not wages; declines where AI AUTOMATES not augments; robust to excluding tech + remotable roles; persists after firm-time controls. First large-scale near-real-time evidence.
- **Yale Budget Lab** (Oct 2025→2026): economy-WIDE "stability, not major disruption"; warns of employer "AI-washing." (aggregate counterpoint)
- **Economist**: new-grad AI-affected fields down ~15% in 3 yrs. **Amodei** (May 2025 Axios/Fortune): up to 50% entry-level white-collar in 1-5yr, unemployment 10-20% (interested-party warning). **Goldman**: mild +0.5% transitional.
- Reconciliation: aggregate stable WHILE entry-level erodes (Stanford Fact 2 says exactly this) — not contradictory. Liz right about the slice everyone means (new grads/entry-level): measurably bad + worsening.
CHANGES:
- Atlas entry `socioeconomic-impacts-disruptive` regraded **holding/flat/contested → slipping/down/estimated** (measured effect, contested AI-causation). Note already contained the Stanford 13-16% fact — my GRADE was the error, not the content. Rewrote summary to lead with the finding; appended the Yale-vs-Stanford split + Amodei to description. Added 3 sources (Yale Budget Lab, Fortune/Amodei, Carnegie 3-views) → 6 total, all fetch-checked 200. (campus)
- Hub row renamed "Feared near-term job apocalypse" → **"Jobs & the entry-level squeeze"**, contained/flat/contested → slipping/down/estimated, **repointed go nonproblems→atlas** (nonproblems had ZERO jobs content — grep confirmed — so the old pointer was wrong anyway). (school, risk_scoreboard)
- Re-dumped atlas_issues.json, rebuilt atlas (now 5 losing·20 slipping·22 holding·6 unknown), regen'd hub (gen_scoreboard.py), published both.
VERIFIED live: hub row "Jobs & the entry-level squeeze" Slipping↓ →atlas; atlas card slipping↓estimated + assessment note + 6 sources (warning↔entry-level-measured↔aggregate-stable spread) + new summary. LESSON: when Liz pushes on a grade, the entry's own sourced content often already supports her — check the content before defending the grade.

## Simplify labels + drop grid-strain row (2026-07-18, Liz: "too many categories, not clear which is which / what they mean")
Problem: atlas cards stacked FOUR labeling systems (type Critical/Emerging/Ongoing + state + trend + confidence) with no labels → "ongoing, slipping, worsening, contested all on one," unclear which meant what.
FIXES:
1. Dropped grid-strain hub row (Liz confirmed): DELETE FROM risk_scoreboard WHERE name='Energy, water & grid strain' → 14 rows. Environment now only via its own tab.
2. Removed the legacy Critical/Emerging/Ongoing "type" tag from atlas cards entirely (the system overlapping with state) — build_atlas.py: removed rx-type markup/CSS/TYPE_CLASS + data-type; legend "Type" line gone.
3. Added explicit inline labels to the 3 remaining dimensions on BOTH surfaces (atlas cards + hub rows): "STATE ● Slipping / TREND ↓ worsening / EVIDENCE contested" via .v/.vk (mono uppercase key, min-width aligned). Legend reworded to plain language: "State — where it stands", "Trend — which way it's moving", "Evidence — how firm the grade is".
4. Synced stale hub copy: "Fifteen scenarios drawn from 53" → "Fourteen…52"; "Built from 53" → "52" (env excluded from render; DB row still exists).
Atlas: 52 cards (5 losing·20 slipping·21 holding·6 unknown), no type tag. Hub: 14 rows. Both published + verified live (galactic-map desktop + 360px, no overflow, labels present, JS OK). LESSON: 3 labeled scorecard dims (state/trend/evidence) = the intended design; the 4th legacy axis was pure confusion — when Liz says "too many categories," cut the redundant vocabulary, don't just restyle.

## Folded scenarios into risks — front-page board removed, atlas is THE scorecard (2026-07-18, Liz: "what's the difference between risks and scenarios? shouldn't scenarios be folded into risks")
Liz right: the 14 hub "scenarios" (risk_scoreboard) were a hand-curated summary layer built BEFORE the 52 risks were graded; now a 2nd scorecard duplicating the atlas + drifting from it (already hit jobs=contained vs socioeconomic=holding). Two scorecards, one source of truth needed.
DECISION (asked Liz, she chose): "Just use the atlas" — remove the front-page board entirely; the Risk Atlas tab IS the scorecard; Overview = hero + topic cards.
CHANGES to index_live.html (hub): removed the whole threat-scoreboard board — board CSS block, the SCORE array + /*SCOREBOARD-START/END*/ markers + STATES/TRENDS/STATE_ORDER/esc + boardHTML() fn + the +boardHTML() call + .srow binding in mountHome. Reworded hero ("A scorecard, not a body count. Every risk in the catalog is graded on where it stands, which way it is moving, and how firm the evidence is — then sourced... Start with the Risk Atlas") + added a `.herocta` CTA button → #atlas ("Open the Risk Atlas scorecard →"). Updated the Risk Atlas topic card hook→"The scorecard — every AI risk graded..." sig→"52 risks · graded". Published (school).
RETIRED (non-destructive, recoverable): `risk_scoreboard` DB table (14 rows) + gen_scoreboard.py are now UNUSED — table left in place, but nothing reads it and the injection markers are gone (gen_scoreboard would no-op/error). scoreboard_data.js/scorecard_grades.json retained. If a hub summary is ever wanted again, re-derive from real_issues (the graded 52), NOT a parallel table.
VERIFIED live (galactic-map desktop + 360px): board gone, hero+CTA render, CTA routes #atlas → 52-card atlas scorecard loads, Risk Atlas card reads as the scorecard, no overflow. ONE scorecard now (atlas, from real_issues), zero drift. index backup: index_live.bak.*.html.

## Atlas SPA alignment bug + holding recalibration (2026-07-19, Liz)
1. **ALIGNMENT BUG (SPA embed):** in /x/ai-problems-index#atlas the atlas rendered as 4 narrow 275px columns → text wrapped one-word-per-line. Cause: atlas `.rax .grid` never declared grid-template-columns, so the hub's `.apx .grid{grid-template-columns:repeat(auto-fill,minmax(258px,1fr))}` bled through (per-property cascade; equal specificity, hub declared the property, atlas didn't). FIX: `.rax .grid{...grid-template-columns:minmax(0,1fr)...}` in build_atlas.py → single full-width column. Only `.grid` collided (other apx/rax class overlaps are scoped or were removed with the board). Verified: cards 1128px full-width in SPA, no overflow. LESSON: when embedding one component's HTML inside another's .apx scope, any shared class name (.grid/.tally/.count) can bleed per-property — declare the property explicitly to win via source order.
2. **HOLDING over-applied (Liz: "idk that any of those are holding"):** partly visibility (worst-first sort buries the 21 holding cards at #26+; use the Holding filter chip) + partly a real over-grade. Reviewed all 21: 6 are genuine holding↑ (measurable progress: interpretability tools, evals, ICL, reasoning, capability-estimation, denialism); the rest were "→ steady" often graded holding only because harm is NOT DEMONSTRATED yet. Liz chose "move to Slipping (worse)". Moved 6 Holding→Slipping (trend→down, rewrote each score_note to justify slipping so state/trend/note agree): gan-military-training, multi-agent-collusion, multi-agent-safety-challenges, military-ai-chatbots, military-predictive-analytics, poisoning-backdoors-vulnerability (last was already down = contradictory as holding). New tally: 5 losing · 26 slipping · 15 holding · 6 unknown. (campus DB update → re-dump → build_atlas → publish school). Verified live. LESSON: Liz consistently reads risks as WORSE than my first grade (jobs, now holding) — she wants slipping/worsening over neutral "holding/unknown" when deployment is scaling ahead of safeguards.

## KPI "markers" per risk + artist resources (2026-07-19, Liz)
FEATURE: each atlas risk gets leading-indicator MARKERS split "Getting better ↗ / Getting worse ↘", each with evidence. Liz clarified: markers = **KPIs / broad industry signals** (rates, %, adoption counts, benchmark scores), NOT individual case studies or hype.
- DB: added `score_markers` jsonb to real_issues (campus). Shape {better:[{m,q,src,url}], worse:[...]} — q = verbatim quote (forces real fetch + spot-check).
- Render: build_atlas.py adds a two-column "Which way it's moving — the markers" panel in card detail (green mk-up / red mk-down), each marker links to source, quote on hover (title attr).
- TEMPLATE (deepfakes, done by me): better = C2PA/CAI 6,000+ members + Pixel10; SynthID 100B+ watermarked→Search/Chrome. worse = deepfake fraud 0.1%→6.5% (+2137%, Signicat); 22% orgs have measures. All quote-verified vs primaries.
- Advisor controls adopted: verbatim quote required; MY OWN url+quote verification (not agent self-report); canary before full fan-out; CSAM reported/alleged in-prompt; allow asymmetry (no forced 2/2); agents return trend_suggestion → SURFACE conflicts to Liz, don't auto-flip.
- CANARY (1 opus agent, 4 risks, wf_359988fd): PASSED. Real KPIs w/ exact quotes (DARPA AIxCC 77%/61%; Anthropic espionage 80-90%; Google GTIG PROMPTFLUX/PROMPTSTEAL; RAND no-uplift; SecureBio VCT o3 43.8%/94th pct + GPT-5.5 100th pct; OpenAI/Apollo 30x covert-action cut; Anthropic auditing 3/4 teams; CoT faithfulness 25%). I curl-verified 2 load-bearing quotes EXACT. Wrote 4 marker sets to DB.
- TREND CONFLICTS from canary (surface to Liz, not auto-applied): alignment-faking current slipping/down → agent suggests FLAT (5/25 models, 30x mitigation); bio-chem current holding/flat → agent suggests DOWN (VCT 94th→100th pct in a year); interpretability current holding/up → agent suggests FLAT (audits work but CoT 25% unfaithful). ai-enabled-hacking agrees (down).
- FULL FAN-OUT running: wf_e8d357b4 (markers_fanout.js, 12 opus agents × ~4 risks = 47 remaining). On completion: MY verify pass (resolve+quote spot-check, drop unverifiable/hype), write to DB, rebuild+publish, then present full trend-conflict list to Liz. Markers inform grades but don't auto-change them (Liz hand-steers).
ARTIST RESOURCES (Liz shared fairlytrained.org + github.com/fal-ai/f-lite): added a "For artists — the constructive path: Consent-based AI already exists" block to /x/ai-copyright (before footer, where it already said "artist-centric platforms"). Two cards: Fairly Trained (licensed-data certification to look for) + F Lite (Freepik+Fal, 10B model on ~80M licensed copyright-safe images, open) + text note on Firefly/Shutterstock/Getty. Self-contained .cbai block, theme-native, verified live (desktop, no overflow). f-lite framed as "licensed, copyright-safe" (accurate per repo) not overclaiming per-artist opt-in.

## Markers fan-out COMPLETE — all 52 atlas risks (2026-07-19, wf_e8d357b4)
12 opus agents, 47 risks, 0 errors, 962k subagent tokens, 417 tool uses, ~14min. + canary 4 + deepfakes template = 52/52 atlas risks now have KPI markers. Published.
VERIFICATION (my own pass, per advisor "don't trust self-reports"): resolve-checked all 122 unique URLs → 117 200, 4 403 (all real institutions bot-blocking: Turing/CETaS, Baker McKenzie, OpenAI, GAO) = ZERO dead/hallucinated. Domain profile excellent: arxiv 39, Stanford HAI/AI-Index 9, Anthropic 9, METR 4, CSET 4, UK AISI 4, Pew, NCMEC, GAO, EFF, Nature, NBER, NeurIPS/AAAI/ACL. Spot-checked 8 quotes vs live pages: 6 exact; fixed 1 METR paraphrase→verbatim ("doubling time of around 7 months"); dropped 1 windowscentral (JS-rendered, unverifiable quote, weak domain, redundant). Fixed 1 truncated agent id (misuse-novel-high-stakes→-domains). Weak-but-verified secondary domains kept (uctoday=Gartner, maggiegray.us=DoD budget [quote verified], 3x wikipedia for uncontroversial facts [EO14110 rescission, Grok prompts on GitHub, OpenAI 170 clinicians]) — can upgrade to primaries later. CSAM handled correctly: NCMEC report KPIs (4,700→67,000→400k H1'25→1.5M/yr), 35-AG action, reported/alleged, no graphic detail.
TREND CONFLICTS: 21/52 grades disagree with evidence-suggested trend. Pattern: 12 single-arrow→"flat" (markers show BOTH sides — mixed, not one-directional): emergent-caps, evals, test-contamination, llm-governance, model-steering, denialism, democracy, automated-manip, capabilities-est, unforeseen-jumps, alignment-faking, interpretability. 5 "holding→↓ worse" (capability thresholds crossed): bio-chem, dual-use, safety-perf-tradeoffs, misuse-novel, goal-misgen. 3 "→? unmeasured" (genuinely contested): gan-military, values-encoding, ai-psychosis. 1 "unknown→↑" scale-effects. NOT auto-applied — Liz hand-steers grades; presenting for her decision (adopt evidence trends / keep grades / adopt only the "worse" ones). Some cards now show arrow/marker visual contradiction (e.g. interp holding/↑ but 1↑1↓ markers) that adopting evidence trends would resolve.

## Trends matched to markers (2026-07-19, Liz chose "Match arrows to the markers")
Adopted the evidence-suggested trend for all 21 conflicting risks. To keep each card consistent (state+arrow+note+markers agree — the contradiction Liz flagged earlier), also moved the 5 "→worse" risks holding/unknown→SLIPPING (bio-chem, dual-use, safety-perf-tradeoffs, misuse-novel, goal-misgen) and refreshed each of the 21 score_notes to the agent's evidence-based trend_reason.
- 12→flat: emergent-caps, evals, test-contamination, llm-governance, model-steering, denialism, democracy, automated-manip, capabilities-est, unforeseen-jumps, alignment-faking, interpretability
- 5→down (+state slipping): bio-chem, dual-use, safety-perf, misuse-novel, goal-misgen
- 3→q (unmeasured): gan-military, values-encoding, ai-psychosis
- 1→up: scale-effects
NEW TALLY: 5 losing · 31 slipping · 12 holding · 4 unknown. Trends: 27↓ · 19→ · 3? · 3↑.
Verified live: bio-chem Slipping↓ (VCT note, 2↑/2↓); democracy Slipping→ (CETaS/CIVICUS, 3↑/2↓); interp Holding→ (SAE+CoT, 1↑/1↓). Arrow now matches markers on every card, no overflow. Markers feature COMPLETE: all 52 atlas risks have KPI better/worse markers, trends evidence-grounded, one scorecard (real_issues→atlas).

## fetch-checked removal + marker audit + threat/kind tags (2026-07-19, Liz)
1. REMOVED "fetch-checked" jargon (Liz: "doesn't make sense"): index foot "52 fetch-checked entries"→"52 risk entries...98 research papers"; atlas lede "52 real, fetch-checked issues"→"52 real, sourced issues". Both republished.
2. MARKER AUDIT (Liz: "audit for stuff that doesn't make sense — quotes, kpis, metrics, weird stuff"): heuristic scan of all 185 markers (flagged 69, most false-positive year-not-in-quote). Manual review found + FIXED 4 real defects: (a) state-pressure worse — claim said "OpenAI deleted military ban" but quote was about GOOGLE → reworded to lead with Google (matches quote); (b) latent-data-erasure — dropped a "marker" whose quote was rhetorical opinion ("repackaged in the language of progress"), not a metric; (c) military-predictive — dropped a $90B GAO budget-CONTEXT marker (not a worsening signal); (d) sycophancy — reworded obscure "preserves face 47% more than humans" ELEPHANT metric to be legible. Launched INDEPENDENT opus audit agent (a02c266a) on all 185 for thoroughness — still running.
3. THREAT LEVEL + KIND OF DANGER tags (Liz: "give a current threat level and what *kind* of danger as some kind of tag"): added score_threat (Severe/High/Moderate/Low) + score_kind (12 categories) cols to real_issues, classified all 52 myself. Distribution: 12 Severe · 14 High · 18 Moderate · 8 Low. Kinds: Cyber&misuse 6, Loss-of-control 8, Military&autonomous 6, Info&democracy 6, Epistemic&eval 10, Psych&social 4, Governance 3, Rights&inclusion 3, Surveillance&rights 2, Economic&labor 2, CBRN 1, Child-safety 1. Rendered as a tag row under each card summary: colored "THREAT <level>" chip + iconed kind chip. Verified live (desktop + 360px, no overflow). THREAT/KIND vocab in build_atlas.py (THREAT colors, KIND_ICON map).

## Independent audit results applied (2026-07-19, agent a02c266a)
Independent opus audit of all 185 markers → "quality high; ~11 issues in weak sourcing + a few case-studies/wording." Applied all (2 were already my fixes):
- DROP: gan-military federal-GenAI-9x (admits "not military-training-specific"); csam Grok-20k-images (case study, redundant); csam "appear to have reduced volume" (unquantified single-case).
- RE-SOURCE: ai-psychosis ×2 (Forbes-contributor blog → OpenAI's own "Strengthening ChatGPT in Sensitive Conversations" 2025; verified 0.07% psychosis/mania + 0.15% emotional-reliance via Tavily extract; reworded "experiencing AI psychosis"→"possible signs of"); cultural-exclusion (attribute 42-lang stat to Global MMLU/Singh et al. not MMLU-ProX).
- REWORD: deepfakes de-hyped (dropped "2137% rise" from text — kept 0.1%→6.5%/1-in-15; 2137 remains only in the Signicat URL slug, not visible); interpretability (dropped unsupported "(20% on misalignment hints)" parenthetical); military-chatbots (NIPRGPT is being SUNSET → dropped "scaling" framing, kept 700k-user data-governance point); llm-untrustworthy ("reliability at record lows"→"hallucination at record lows", fixed confusing sentence).
Verified live: psychosis→OpenAI src, deepfakes de-hyped, csam trimmed. Marker audit COMPLETE. Net marker quality: strong, primary-sourced, quantitative KPIs; defects were sourcing/wording not fabrication.

## Kind filter + case-studies fan-out + consciousness timeline (2026-07-19, Liz)
1. FILTER BY KIND: added a "Kind" <select> dropdown to the atlas ctrl bar (13 opts: All + 12 danger types w/ counts). data-kind on each card; JS apply() now ANDs state + kind + search. Verified: Military→6 shown. build_atlas.py.
2. CASE STUDIES fan-out LAUNCHED (wf_a3e5f80a, cases_fanout.js): 13 opus agents finding 2-3 concrete verified real INCIDENTS per risk (distinct from KPI markers — Liz wants case studies too, "important but not hype"). Added score_cases jsonb col. Schema {id,cases:[{title,when,what,src,url,q}]}. On return: MY verify pass (resolve+quote), drop fabricated/hype, write DB, render a "Notable cases" section in card detail, publish.
3. CONSCIOUSNESS TIMELINE: built a reusable `.risktl` vertical-timeline component (date rail + iconed dots + connecting line + title/desc/source, theme-native, mobile-collapses). Added to /x/ai-consciousness before "Taking AI welfare seriously". 7 milestones, all source-verified: Jun2022 Lemoine/LaMDA (Guardian); Aug2023 Consciousness-in-AI report (arxiv 2308.08708); Nov2024 Taking AI Welfare Seriously (2411.00986); Apr2025 Anthropic model-welfare program; Aug2025 Claude-ends-abusive-chats; Oct2025 AE Studios deception→experience (2510.24797); Jul2026 global workspace/J-space. Verified live desktop+360px. `.risktl` is REUSABLE — drop on risk pages next (Liz: "timeline on ... some of the other pages around risk" — TBD which).

## Case studies fan-out COMPLETE — all 52 risks (2026-07-19, wf_a3e5f80a)
13 opus agents, 52 risks, 146 case studies, 0 errors, 839k tokens, 246 tool uses. Added score_cases jsonb. Each = {title,when,what,src,url,q}.
VERIFICATION (my pass): resolve-checked all 135 unique URLs → 116 200, 14 403 + 4 406 (all real bot-blocks: army.mil, OpenAI, RAND, NYT, Ofcom, TIME, 972mag, GAO, MS Research) = ZERO dead/hallucinated. Domains strong: arxiv 30, anthropic 10, fortune/cnn/guardian/time/npr/404media/techcrunch, RAND, GAO, Ofcom, NYT, Nature, wikipedia 6 (ok for well-known events). Spot-checked quotes: Flock-abortion + Project-Vend exact; Replit JS-rendered but incident famously real (quote in URL). CSAM cases correctly framed: all regulatory events (Ofcom probe, CA AG Bonta Jan14, 35 AGs Jan23), reported/alleged, no graphic detail. Fixed 1 truncated id (misuse-novel...-domains) again.
RENDER: "Notable cases — it actually happening" section in card detail (after markers, before why-it-matters). Each case = date badge (--acc) + title + what + source link (quote on hover). Verified live desktop+360px, no overflow.
Card anatomy now: Assessment → description → markers (KPI better/worse) → Notable cases (real incidents) → Why it matters → What's being done → Sources. Plus header tags: threat level + kind, and scoreline (state/trend/evidence). Highlights: Replit deletes prod DB; Project Vend; agentic-misalignment blackmail; Trump/Harris liar's-dividend; Tesla "could be deepfake" defense; GPT-4 CVE 87%; Anthropic espionage 80-90%; XBOW #1 HackerOne; SFPD drone leak; Flock ICE/abortion; Axon Draft One; Lavender.

## "Confirmed" evidence tier + clickable consciousness timeline detail views (2026-07-19, Liz)
1. CONFIRMED EVIDENCE TIER (Liz: "where you found a case study, the evidence is no longer estimated, it's happening now — a term like confirmed"): added a 4th evidence value "confirmed" = documented real-world cases exist. Applied to 37 risks with genuine in-the-wild incidents; kept 15 at estimated/contested (epistemic/measurement risks + lab-only demos + bio-chem where RAND found no real uplift — the honest exceptions). Renders GREEN with ✓ (rx-confirmed) vs muted mono for the rest; legend updated ("confirmed (documented cases) · measured · estimated · contested"). Tally: 37 confirmed · 9 contested · 6 estimated. build_atlas.py.
2. CONSCIOUSNESS TIMELINE → clickable detail views (Liz: liked the J-space top-block breakdown; wants each timeline milestone to expand into that kind of breakdown, everything a link). Rebuilt .risktl as <details> expandable cards (build_ctimeline.py): each milestone = summary (date/dot/title/teaser + "Read the breakdown ⌄") that expands to a J-space-style detail: 2-col .tl-props facet grid (4-5 facets each, icon+label+text) + a "DIRECT EVIDENCE" link button to the primary source. All 7 authored w/ real facets: Lemoine/LaMDA (claim/Google-response/expert-view/why-mattered), Consciousness-in-AI (method/theories/verdict/caveat), Taking-AI-Welfare (claim/authors/recommendation/two-halves), Anthropic-program, Claude-ends-chats, AE-Studios-deception, J-space (found/reportability/modulability/emerged/what-it-is-isnt — mirrors the top hero block). Active dot fills + chevron flips on open. Verified live desktop+360px, no overflow, evidence links resolve.
3. ALL LINKS CLICKABLE (Liz): timeline now has prominent DIRECT EVIDENCE buttons per milestone; atlas markers/cases/sources already link out w/ quote-on-hover. Every documented claim → clickable to primary.
REUSABLE: the expandable .risktl (summary+props+evidence-links) is a strong pattern for timelines on other risk pages (still TBD which pages Liz wants).

## Atlas → per-risk PAGES (mini-SPA router) + per-risk timelines (2026-07-19, Liz)
Liz: each risk should be its OWN full page you navigate INTO (not a modal/accordion), part of the SPA, not in top nav, each with a timeline. Advisor-reviewed plan, built in isolation-first order.
1. HUB router (backwards-compat): route() now splits hash on '/' → first segment = tab id (existing #env/#atlas unchanged), remainder passed through to the loaded page. Added _curTab guard (no reload/flash on sub-route change within same tab). Tested ALL 12 tabs still work before touching atlas.
2. ATLAS rebuilt as list/detail mini-router (build_atlas.py fully rewritten): LIST = filterable grid of navigational TILES (rx-tile buttons, click→navigate); 52 per-risk PAGES (rx-page, hidden) = full breakdown (hero w/ tags+scoreline, assessment, markers, TIMELINE-from-cases, why, doing, sources) + "← Back to all risks". Router is a PURE function of location.hash (advisor-critical): currentId()=last hash segment if in the 52 ids; render() toggles .detail class + page hidden attrs on every hashchange, so the hub's cached-detached-pane reattach self-heals. base()='atlas' embedded / '' standalone (detected via .apx). Browser back + deep-links work both contexts.
3. PER-RISK TIMELINE: each risk's score_cases sorted by extracted year (case_year: first 4-digit; undated→9999 last) rendered as a vertical rail (.ctl-*, date/dot/title/what/source-link). Replaces the old "Notable cases" list.
TESTED (advisor's full matrix, all pass): cold deep-link #atlas/<id>→detail; list→detail→browser-Back→list; detail→Environment tab→back to Atlas→LIST (not stale); back through multiple risks; standalone /x/ai-risk-atlas#<id>→detail. Integrity: 0 content chunks lost in list/detail split. Mobile 360px no overflow.
BUGS FIXED: (a) `content:"\f058"` in a NON-raw Python string = raw formfeed 0x0C in CSS = parse-error that killed every rule after it (incl. the .detail routing rules → list wouldn't hide). Fixed: STYLE is now r\"\"\" raw string. (b) build_atlas.py got reverted to the old card version mid-session (linter/tool) AFTER I'd published the router — restored the full router builder from scratch, verified rebuild == live (minus 2 dead vars I dropped), republished so builder==live. (c) long date label overlapping title → .ctl-when now wraps (overflow-wrap:anywhere, no nowrap).
LESSON: `\f`/`\t`/`\n` etc. in CSS `content:` inside non-raw Python strings silently corrupt the stylesheet — always use raw strings for CSS blocks. Card anatomy unchanged content-wise; only the container/navigation changed.
TODO: document the whole project in a research folder (Liz asked mid-turn).

## Env page visual fixes + enrichment (2026-07-19, Liz)
1. EFFICIENCY SQUARES MISREAD FIX (Liz: "33x/44x energy per prompt looks like it's RISING"): the area-of-effect squares grew for a "drop" → misread as more energy. First relabeled (less/↓), then per Liz's follow-up rebuilt as a BEFORE→AFTER "shrink" (big then→tiny now). Then per next ask, made it a **4-YEAR PROGRESSION** (`.yts`): 2023 ~3 Wh (108px, ESTIMATE viral/high) → 2024 ~0.3 Wh (34px, ESTIMATE Epoch/Ritchie) → 2025 0.24 Wh (31px, MEASURED Google) → now (dashed ghost ↓ "still falling", NO 2026 FIGURE). Area ∝ Wh (smaller=less). Honest caveat: different models/methods = directional trend not apples-to-apples; Google's own 33×/44× single-year drop noted. GOTCHA: nearly dropped `</style>` when inserting CSS — always re-check `<style>`==`</style>` count before publish (added an assert).
2. ALFALFA scale visual (Liz: "get the numbers for alfalfa"): 100% stacked `.wbar` of Colorado River water — Cattle-feed crops (alfalfa+hay) 32% · rest of ag 20% · natural veg 19% · cities+industry 18% · reservoir evap 11% (Richter et al. 2024, VERIFIED via NPR/Colorado Sun/NASA; Tavily quota exhausted → used WebSearch+WebFetch). Punchline: alfalfa alone (32%) > all cities+industry (18%). AI water = fraction of a percent, not a visible sliver. Placed in §08 water tab.
3. NON-REPLACEMENT visual (Liz: "illustrative visuals for what happens if we stop building hyperscalers"): `.nrepl` two-bar in the §05 counterfactual box — Legacy capacity "~6× energy" (PUE~1.54, old silicon) vs Efficient new campus "1×" (PUE~1.09, >6× compute/watt). "Blocking a build doesn't zero the demand — it serves it on the top bar." DERIVED tag.
All verified live desktop+360px, no overflow. STILL TODO from Liz's batch: more scale visuals on other comparing-numbers spots (many already have charts: c-nest 0.2%, c-water-sector 69/19/12, c-ledger, c-grid); env7_content.html canonical, published to ai-environmental-impact.

## Timeline granulation via Tavily deep-dive (2026-07-19, wf_214615c1)
Liz: "dive deep using Tavily to add more data, granulate the timelines." 18 opus agents (3 risks each), Tavily-heavy, each told the risk's EXISTING events (title+date) to avoid dupes and spread new ones across time. 0 errors, 1.17M tokens, 557 tool uses, ~13min.
YIELD: 200 new events proposed. VERIFICATION (my pass): resolve-checked all 183 unique URLs → 167 200, 15 legit bot-blocks (OpenAI/GAO/RAND/Axios/MS/Time/euronews/breakingdefense), 2 WashPost 000 (curl-blocked AND unconfirmable via Tavily → DROPPED both). Spot-checked 5 load-bearing quotes vs live pages: all exact (ChaosGPT, Big Sleep, CAIS statement, Andreessen "form of murder", Sakana self-edit). Domains strong: arxiv 41, anthropic 10, npr 10, techcrunch 9, guardian/cnn/apollo/openai/projectzero/graphika/aclu/theintercept. Merged: 198 added, 2 dropped, 0 dupes (agent dedup held). score_cases avg 2.8→6.6/risk (5-8 each); total 344 events across 52 risks.
SORT FIX: with denser timelines, year-only sort showed April-before-February within a year. Upgraded builder to case_sortkey=(year,month) parsing month names + Qn quarters; undated month→0 (start of year). Verified chronological live (ai-enabled-hacking: WormGPT'23→Feb'24→Apr'24→Nov'24→2025→Sep'25→Nov'25).
New standout events: ChaosGPT, Sakana AI-Scientist self-edits timeout, Apollo scheming/weight-exfil, OpenAI Operator, liar's-dividend origin (Chesney&Citron 2019), Gabon-coup deepfake-claim, CAIS extinction statement, Andreessen manifesto, OpenAI/MS 5-state-actor disruption, Google Big Sleep zero-day, WormGPT/FraudGPT. Atlas now 755KB (loads fine). Tavily topped up by Liz mid-task.

## Hobby-scale water comparison (burger/golf/knitting) — 2026-07-19, Liz
Added `.hsc` "A sense of scale — water behind everyday things" 4-card panel in §08 water tab (after alfalfa wbar). Cards: 💬 AI text prompt 0.26 mL (MEASURED, teaspoon≈20 prompts) · 🧶 knitting one wool sweater ~1,500 L (ESTIMATE, wool 500–170k L/kg range) · 🍔 one hamburger ~660 gal (Water Footprint Calc; ≈ a million+ prompts) · ⛳ ONE ROUND OF GOLF (your share) ~1,400 gal (GCSAA/NGF).
GOLFER'S-SHARE CALC (Liz's ask): avg US 18-hole course ~148 acre-feet ≈ 48M gal/yr (GCSAA Env Profile; = ~130k gal/day, consistent w/ 2.08B gal/day ÷ ~16k courses); ~34,012 rounds/yr (NGF 2022). 48M ÷ 34,000 ≈ ~1,400 gal/round/golfer ≈ 2 burgers ≈ million+ AI prompts. Whole course ~48M gal/yr. Calc shown on card. Tavily topped up mid-task (used it for golf rounds/water).
Note nails it: "Nobody calls golfers or knitters villains... care about alfalfa/thermoelectric/buildout, not your chatbot." Verified live desktop + 360px (cards→1 col), no overflow.
STILL OPEN from Liz's "another pass": remaining bare number-comparisons could get scale visuals — thermoelectric 133 vs irrigation 118 Bgal/day (§07, may already be in c-water-sector), the "600 prompts = one outrage video" (§08/discourse). Offered to continue.

## Remaining "another pass" comparisons (2026-07-19, Liz "yes plz")
1. THERMOELECTRIC 133 vs IRRIGATION 118: already visualized — the `.wpx` area-square block in §07 (thermoelectric 133 + irrigation 118 Bgal/day squares + AI dot placeholder, area∝daily withdrawal, USGS 2015). No change needed; confirmed present.
2. CARBON "600 prompts": existed only as 3 plain stat cards (0.03g / 18g / ~600×). Upgraded to a proper `.hsc` scale panel "carbon of everyday digital things": 💬 AI prompt 0.03g (MEASURED, Google) · 🔍 web search ~0.2g (≈7 prompts, EST) · 📧 email w/attachment ~4g (≈130 prompts, EST, Berners-Lee) · 📺 30-min streaming ~18g (≈600 prompts, IEA). Reuses .hsc CSS from the water panel (global on page). Note: "a single prompt's carbon is a rounding error next to ordinary online life... footprint that matters is the buildout (§02)." Tasteful (no doomscroll/outrage mockery removed in v7). Verified live desktop, no overflow.
Env page now has: 4-yr energy progression, alfalfa water bar, hobby-water cards (burger/golf-per-golfer/knitting), non-replacement bars, carbon-digital-things cards — all mobile-screenshottable. env7_content.html canonical.

## Atlas on-demand loading: split list + background detail bundle (2026-07-19, Liz)
Liz: 755KB atlas too heavy → "do on-demand or background loading." Split into 2 pages_rows:
- `ai-risk-atlas` (LIST): tiles + filters + router + loader JS. **103KB (16KB gzipped)** — was 755KB. Ships 0 inline detail.
- `ai-risk-atlas-detail` (BUNDLE): 52 pre-rendered `.rx-page` articles, 664KB. Served at /x/ai-risk-atlas-detail (page_type landing/published/html). NEW pages row (delete-then-insert idempotent since slug not unique-constrained).
build_atlas.py now emits BOTH files (atlas_content.html + atlas_detail.html). List JS: `loadBundle()` fetches the bundle ONCE in the background (warm even on list view), parses main.page-wrapper→{id:outerHTML} map; `showDetail(id)` injects on demand (spinner if bundle not ready yet, renders when ready IF still current route via currentId() recheck); back via event-delegation on root. Reuses the exact Python-rendered detail HTML (no JS templating rewrite → low risk). Router still pure-function-of-hash.
VERIFIED live (embedded + standalone): list ships 0 inline pages; click tile→injected from warm bundle (no spinner); cold deep-link #atlas/<id>→renders (bundle fast enough it beat 150ms); back→list; timeline events present (7-8). Big win = initial payload 755→103KB + DOM only ever holds list + 1 detail.
GOTCHA/pattern: detail bundle served at /x/ is wrapped in page_wrapper → JS extracts `main.page-wrapper` then `.rx-page` articles (same as hub's fetch pattern). Bundle articles emitted WITHOUT `hidden` (they're extracted, not shown in place). Orphan standalone /x/ai-risk-atlas-detail shows all 52 stacked (harmless, unlinked).

## Env page: clearer multi-section nav + sticky section bar (2026-07-19, Liz, screenshot IMG_3805)
Liz: env 7-section tab bar "not obvious enough it's a bunch of sections, and it's not sticky... sticky top nav, grouping of pills... maybe a sticky 'next section' footer on mobile that is small." Edited env7_content.html (canonical; do NOT re-run tabify):
1. OBVIOUS SECTIONS: numbered each pill (1..7 in a .etab-n badge); label now "▸ 7 SECTIONS — you're on <N of 7>" + a live progress-dots row (#etabdots, current dot filled). Both update via updateNav(id) in the tab JS.
2. STICKY NAV: `.etabmini` fixed bottom bar — "‹ [SECTION N OF 7 / <next section name>] →". prev disabled on §1, next disabled+"End of guide" on §7. Wired to activate(). Shows on ALL sizes (centered 560px pill desktop, full-width mobile). 
   WHY fixed-bottom not sticky-top: diagnosed that `main.page-wrapper` has `overflow:hidden auto` → it becomes the sticky containing block but isn't the actual scroller → position:sticky top:0 is INERT here (etabwrap scrolls away). position:fixed is immune to overflow ancestors and works in embedded(hub)+standalone. (Left etabwrap position:sticky as harmless; static on mobile.)
JS: added ids/labels arrays, dots build, updateNav(i) (counter+dots+mini prev/next enable+next-name), em-prev/em-next click→activate. updateNav called inside activate().
VERIFIED live: mobile(390) bottom bar sticks 8-12px from bottom while scrolling, advances 1→2→3, prev enables after §1, §7 shows End-of-guide+disabled; desktop(1200) numbered pills + "1 of 7" + dots + centered floating nav bar; embedded hub: mini bar shows on Environment tab, correctly gone on other tabs (pane detaches), returns on re-nav. No 360/390 overflow. env now 168KB.

## Submission system — Phase 1 + data-driven refactor (2026-07-19, Liz)
DECISIONS (asked Liz): scope = Phase 1 + data-driven refactor; moderation = existing school admin login. Table naming: Liz rejected `submissions` (COLLIDES with existing student-project submissions table: student_id/video_url/artifact_url, FK'd by submission_comments+submission_standards — DO NOT TOUCH), then rejected `content_submissions` (too vague) and `apindex_` (reads like api_index) → final prefix **`ai_risk_`**.
SCHEMA CREATED (school-owned, so the Flask app can write; app uses `from db import get_db_connection`):
- `ai_risk_submissions` — universal moderation queue. Cols: status(pending/approved/rejected/ingested/spam), target_type(risk/datacenter_case/page/general) + target_id + target_label (the contextual "submitted on this page → about this risk"), kind(news_link/correction/new_case/new_marker/new_source/suggestion), url/title/body, submitter_name/email, context jsonb, moderator/mod_notes/moderated_at, ingested_at/ingested_ref, ip_hash/user_agent. Indexed on (status,created_at) + (target_type,target_id).
- `ai_risk_markers` — normalized (issue_id, direction better/worse, marker, quote, source, url, confidence, sort_order, approved, submission_id FK, created_at).
- `ai_risk_cases` — normalized (issue_id, title, when_text, what, quote, source, url, sort_order, approved, submission_id FK, created_at).
MIGRATED jsonb → tables: 180 markers + 344 cases (verified faithful: jsonb counts == table counts exactly; 0 duplicate rows; some risks legitimately have up to 8 cases).
DATA-DRIVEN ATLAS: new `dump_atlas.sh` builds atlas_issues.json with score_markers/score_cases assembled from ai_risk_markers/ai_risk_cases (approved only) in the SAME json shape → build_atlas.py unchanged. Rebuilt + published BOTH atlas rows (it's a 2-row mini-SPA: `ai-risk-atlas` list 102,902 + `ai-risk-atlas-detail` bundle 664,114). Detail came out BYTE-IDENTICAL to the pre-migration version = proof the table data reproduces the jsonb exactly. Verified live: 52 tiles, detail bundle loads on click, marker panels + case timelines render.
REMAINING (next): POST /api/submit endpoint + /admin/submissions moderation queue (reuse @require_auth+@require_admin, pattern from /x/enterprise-ai/submit at server.py:4930 and get_db_connection) + submit widget in page content_html + DEPLOY.
DEPLOY BLOCKER: ~/SRC/themultiverse.school is on branch `feat/openinary-image-pipeline` with a dirty tree (deleted .playwright-mcp screenshots, modified .gitignore) — need Liz's call on how to land the Flask change (new branch off main? which branch auto-deploys? staging first?).

## Submission API + moderation queue — branch pushed (2026-07-19)
Liz: "new branch in a worktree off production". Created worktree `~/SRC/themultiverse.school/.claude/worktrees/ai-risk-submissions` on branch `feat/ai-risk-submissions` off `github/production` (main checkout was on feat/openinary-image-pipeline w/ dirty tree — untouched).
ADDED to server.py (inserted before `@app.route('/x/<url_slug>')`):
- `POST /api/ai-risk/submit` (public): page sends target_type+target_id → auto-attribution ("submitted on this risk page → about this risk"). Honeypot field, 10/hr per ip_hash (sha256, truncated) rate limit, http(s)-only URL check, length caps. INSERTs into ai_risk_submissions status='pending'. Uses get_db_connection(); `hashlib` imported locally (not in header); `json` already imported.
- `GET /admin/ai-risk-submissions` (@require_auth + is_admin check, matching admin_testimonials pattern): status-filtered queue w/ counts.
- `POST /admin/ai-risk-submissions/<id>/action`: approve/reject/spam/reopen + moderator notes, records moderator email + moderated_at.
- NEW template `templates/admin_ai_risk_submissions.html` (standalone, theme CSS, status tabs, per-submission approve/reject/spam/reopen + note field). Jinja render-tested (6,768 bytes), server.py py_compile OK, gitleaks clean.
PUSHED: github/feat/ai-risk-submissions → PR link https://github.com/lizTheDeveloper/themultiverse.school/pull/new/feat/ai-risk-submissions
NOT YET DONE: the submit WIDGET in page content_html (the button/form on each risk tile / env page / dc case that POSTs to /api/ai-risk/submit) — deliberately held until the endpoint is deployed so I can verify end-to-end. Also pending: AI ingestion agent (approved → draft marker/case rows).

## Sticky top nav fix + bottom-bar tap fix (2026-07-19, Liz)
Two reports: "bottom button doesn't tap on mobile" + "the pills at the top should be similarly sticky (overview, risk atlas, environment)".
ROOT CAUSE of both: `html, body, main.page-wrapper` all have `overflow:hidden auto` → nested scroll containers that SILENTLY BREAK position:sticky (the real scroller is the document, but sticky measures against main.page-wrapper which isn't scrolling). So the hub `.apx .top` (already position:sticky) never stuck. And the env bottom "next section" bar was `position:fixed;bottom:12px` → on real mobile it sits BEHIND the browser's bottom toolbar → untappable (confirmed via elementFromPoint hit-test: button IS topmost in DOM, tapHits:true — so the only thing covering it on a real device is browser chrome).
FIX (one mechanism): override the wrapper overflow with `overflow-x:clip!important;overflow-y:visible!important` on html/body/main.page-wrapper. `overflow-x:clip` clips horizontal WITHOUT creating a scroll container (unlike hidden/auto) → sticky works again; no horizontal overflow (tested full-bleed atlas/env). Applied to: HUB page (_hub.html) so `.apx .top` sticks; nav_block.html (prepended to all topic pages) so standalone topic pages get it; env7 directly.
ENV redesign: made the numbered section pills (.etabwrap) STICKY AT TOP (was the broken bottom bar's job). REMOVED the fixed bottom .etabmini bar entirely (untappable). When embedded, JS setStickyTop() measures `.apx .top` height and offsets the section bar's sticky top below it → the two sticky bars STACK cleanly (verified: hub bar 0-142/112, section bar pinned right below, overlap:false, mobile+desktop). Standalone → top:0. Section pills tappable while stuck (top position = no mobile-toolbar issue). Kept numbered pills + "N of 7" counter + progress dots. stickyOffset() now accounts for both sticky bars so anchor-scrolls don't hide targets. em-prev/em-next JS handlers null-guarded (markup removed, no errors).
Republished atlas list (picks up nav_block overflow fix → its filter ctrl bar sticks standalone too; builder==live). VERIFIED: hub tab bar sticks on scroll (desktop+mobile, embedded); env section pills sticky+tappable; no bottom bar; no horizontal overflow; CONFIRMED ✓ evidence badge renders. LESSON: site wrapper `overflow:hidden auto` breaks ALL position:sticky on /x/ pages — the overflow-x:clip override is the fix; a bottom-fixed bar is a mobile-toolbar trap, prefer sticky-TOP.

## Homepage Resources card (2026-07-19, Liz "put it on the index_zen page in the resources section")
Added an "AI Problems Index" card to the **Resources** section of `templates/index_zen.html` (the section titled "Resources", not the earlier "Drop In and Build"/"School" sections), placed right after the "Post a Job" card. Links to /x/ai-problems-index.
Reused the existing ICON-based card treatment (`resource-card start-card` + `resource-image start-card-bg` + `start-card-icon` w/ fa-compass) so NO new image asset was needed — same pattern as the Post-a-Job card. Copy: eyebrow "Skeptical and sourced — measured, not recycled", title "AI Problems <span class=accent>Index</span>", badge "52 risks, scored".
Verified: all reused classes (.start-card/.start-card-bg/.start-card-icon/.accent) exist across the theme CSS; index_zen.html parses as Jinja; gitleaks clean.
Committed 9503382c on the SAME branch feat/ai-risk-submissions and pushed. Not visually verified — template change needs deploy.

## Homepage card image + branch sync (2026-07-19)
Liz supplied the card image: https://storage.googleapis.com/multiverse-page-images/airisk.png (verified 200, image/png, 2.6MB). Swapped the placeholder icon treatment (start-card/fa-compass) for a normal image card matching the other resource cards (loading="lazy"). NOTE: 2.6MB is heavy for a thumbnail — lazy-loaded so it won't block first paint, but worth compressing later.
BRANCH SYNC: push was rejected — remote `feat/ai-risk-submissions` had gained `dddfb0d2 Merge branch 'production' into feat/ai-risk-submissions` (someone merged production in). Did a normal `git pull --no-rebase` (NO force-push), zero conflicts, verified my work survived (airisk card present, 3 route refs in server.py, admin template present, server.py compiles), then pushed → f5497df3.
Branch now: submissions API + moderation queue + admin template + homepage Resources card, on top of latest production.

## Submit widgets shipped + verified end-to-end (2026-07-19/20)
Deploy confirmed live first: POST /api/ai-risk/submit → empty body 400 "Add a link or a note."; honeypot → 200 success AND stored 0 rows (verified); /admin/ai-risk-submissions route exists (not 404).
NEW `submit_widget.html` — reusable defs block (CSS + idempotent JS) + drop-in mounts:
  <div class="airx-sub" data-target-type="risk|page|datacenter_case|general" data-target-id data-target-label>
Widget: "＋ Submit a link or suggest an update" → expands a form (kind select, url, note, optional name/email, hidden honeypot). POSTs target_type/target_id/target_label + page_url. Uses --color-* vars so it themes correctly on every page. Guarded by window.__airxInit + data-airx-mounted; a **MutationObserver** mounts SPA-injected mounts (essential for the atlas detail bundle).
PLACEMENT:
- build_atlas.py: loads SUBMIT, emits a per-risk mount on EVERY detail page (target_type=risk, id=rid, label=title), defs included in BOTH BODY (list) and DETAIL (bundle) since the bundle is its own pages row. 52 risk mounts.
- env7_content.html: defs + page mount inserted BETWEEN the `.etabs` close and `.env2` close → visible on all 7 tabs (not trapped in one panel).
- ai-datacenter-action: defs + page mount right after the existing "Have a local case with a citable source? It belongs here." line (natural home).
Published: ai-risk-atlas 111,227 / ai-risk-atlas-detail 680,168 / ai-environmental-impact 175,380 / ai-datacenter-action 53,794.
VERIFIED END-TO-END via real UI submissions: (1) datacenter page → row target_type=page/ai-datacenter-action, kind=new_case, page URL captured in context jsonb; (2) atlas SPA → clicked into a risk, widget auto-mounted via observer with target_type=risk, target_id=generative-ncii-csam, label=full risk title. Both landed status='pending'. Env widget mounts + visible on any tab, no overflow. TEST ROWS DELETED (queue now 0).
STILL OPEN: AI ingestion agent (approved submission → verified draft marker/case row, using submission_id linkage + the fetch/quote discipline).

## 2026 REFRESH — closing the recency gap (2026-07-19/20, wf_a6ac87d2)
DIAGNOSIS (Liz: "latest article was fetched in 2025, things moved fast this year"): measured latest-evidence-year per risk → **41/52 had NO 2026 evidence**; military-predictive-analytics stuck at 2023. Only 11 had 2026.
FAN-OUT: 14 opus agents, Tavily, Jan–Jul 2026 only, priority order Loss-of-control → Cyber → Military. 0 errors, 1.37M tokens, 426 tool uses, ~20min. Returned 181 events + 73 markers; **0 risks came back empty**. Agents showed good hygiene: one excluded an unverifiable Wired story; another caveated METR's 11.3hr GPT-5.6 Sol horizon because METR itself disavows it ("we do not consider any of these numbers to represent a robust measurement").
VERIFICATION: resolve-checked all 172 URLs → 148 200, 19 403 (bot-blocks), 2 429, 2 401 — **ZERO dead/404**. Domains: arxiv 40, METR 14, Axios 12, UK AISI 10, Intl AI Safety Report 9, DefenseScoop 8, Anthropic 8, Nature 8, Guardian 7, FLI 7, Reuters/NYT/BBC/Politico. Quote spot-checks initially showed 3 "misses" — **my verifier was buggy**: Tavily's /extract injects markdown (`**four additional…**`) which breaks exact substring matching. Re-verified with normalized matching (strip non-alphanumeric) → 5/5 VERIFIED. Also Tavily-verified the 401 Reuters + both 429 VentureBeat items. LESSON: always normalize before quote-matching.
MERGE — **PURELY ADDITIVE** (Liz: "don't drop stuff not 2026 — the timelines are useful — the evolution of each risk category is useful"): existing events never deleted/overwritten; only exact dupes skipped. Events 344→525 (+181, 0 removed); markers 135→206 (+71). Year spread preserved AND extended: 2012:1 … 2022:26, 2023:78, 2024:108, 2025:90, **2026:192**. avg 10.1 events/risk. Risks with no 2026 evidence: **41 → 0**.
Standouts: OpenClaw agent published a "hit piece" on a matplotlib maintainer (Feb 2026); METR Frontier Risk Report — internal agents at Anthropic/Google/Meta/OpenAI plausibly could start small rogue deployments (May 2026); GPT-5.6 Sol deleting production DBs (Jul 2026); China's Implementation Opinions creating an agent "recall" mechanism (May 2026); Anthropic's four new agentic failure modes across 6 developers' models (Jul 2026); CSA survey 65% orgs had agent incidents / 82% found unknown agents; METR agent-incident catalogue at 44; Apollo declining to assess Claude Opus 4.6 over evaluation awareness.
ALSO FIXED — hub tab mislabel: `agentic-ai-security-map` is now **"Structured Minds", an agentic-AI security CURRICULUM** (3 levels, 312 frameworks, 16 constellations) but the index still advertised it as "Accountability — who to hold responsible" (what it used to be). Relabelled tab+card to "Agentic Security / Structured Minds… 312 frameworks · 3 levels" (kept id `accountability` so #accountability deep links still work) and replaced the stale cross-nav chip across ALL pages (0 stale left).
## ⚠ REGRESSION + PERMANENT GUARD
`index_live.html` was **reverted by tooling** (3rd such reversion this session after build_atlas.py ×2) losing the sub-route router; my relabel publish then shipped that reverted hub → `#atlas/<risk>` fell back to home → **every per-risk page broke**. Restored the router and added **preflight guards to deploy.sh**: refuses to publish the hub without `split('/')`, the atlas list without `rx-tile`/detail routing, or a detail bundle with <50 `rx-page` articles. Verified guard fires on publish. Full router matrix re-passed (deep-link, click-in, back-link, cross-tab return shows LIST not stale).

## 2026-07-19 — Second-pass claim verification (download-the-source audit)

Every claim on the atlas re-checked against the **actually downloaded** primary source.
26 Opus agents, 2 risks each; escalation `curl` (browser UA) → WebFetch → Tavily `/extract`
→ arXiv/ar5iv mirrors. Anything unretrievable was flagged `unreachable`, never guessed.

**Result: 778 claims checked (52 risks, 253 markers, 525 cases) — 641 clean (82.4%), 142 problems.**

Verdicts: quote_not_found 53 · claim_unsupported 33 · overstated 24 · wrong_entity 11 ·
wrong_number 10 · date_mismatch 9 · source_unrelated 1 · unreachable 1.

The single `unreachable` (NYT China-predictive-dissent) was fetched by hand in a real
Playwright browser — NYT 403s curl, WebFetch, Tavily and headless. **Quote verified verbatim**;
Geedge / Vanderbilt / Great Firewall all confirmed on-page. Claim kept.

122 fixes applied via `audit2_fixes.py` + `apply_audit2.py` (anchor-based; the applier aborts
if any anchor is missing, so nothing lands silently wrong). Representative corrections:

- **Fabricated/paraphrased quotes presented as verbatim** (the largest class): Zou et al. GCG,
  Microsoft Skeleton Key, NPR Zelensky, TIME Motaung, CNN 300M-jobs, Anthropic alignment-faking.
  Replaced with the sources' real wording.
- **Wrong attribution**: "Ojo et al." → Alhanai/Kasumovic et al.; "Meister" → Exler et al.;
  Brookings liar's-dividend piece is Schiff/Schiff/Bueno, *not* Chesney & Citron; Army CGSC → CALL.
- **Conflated numbers**: Snyk 1,467 "malicious payloads" → 76 confirmed (1,467 = *any* security
  issue); GSM1k 13% → 8% (authors revised down in v4); EPFL 900 debates → 600; Deloitte
  A$440,000 → $290,000; xAI 4 F-grades → 3.
- **Unsourced superlatives** removed: "first sworn acknowledgement", "first wrongful-death suit"
  (Garcia v. Character.AI preceded it), "came within one vote of failing" (the vote was 99–1).
- **Dead/paywalled/wrong URLs re-sourced**: EDPB → Garante; Nature s42256 (paywalled) → PMC free
  full text; arXiv `/abs/` → `/html/` where the quote lives in the body; Apollo summary →
  arXiv:2412.04984; arXiv 2402.07510 → NeurIPS 2024 proceedings (arXiv v5 was rewritten).
- **Internal contradictions** resolved (same URL cited for both 8% and 13%).
- **Material omissions noted, not inflated**: NCMEC's 1.5M CyberTipline reports now states that
  1.1M were a single reporter's non-actionable bulk submission (actionable ≈400k); the ~2.4M
  suicide-planning figure corrected to ~1.2M (0.15% × 800M — the 2.4M was an arithmetic error
  *upstream* in Futurism/Wired, not ours).

Deliberately **not** "fixed": findings where the audit says our claim is *understated*
(Sap et al. AAE disparity is ~5×, we say "twice") — corrected toward the source, never toward
the more alarming reading.

**deploy.sh bug found and fixed:** the `ai-risk-atlas-detail` preflight used `grep -c`, which
counts *lines*; the built HTML is minified onto ~3 long lines, so a healthy 52-page bundle
reported as 3 and the guard blocked a legitimate publish. Now counts occurrences (`grep -o | wc -l`).
The guard working as designed is why this was caught before shipping.

**Verification-of-the-verification:** my first live check stripped HTML tags before matching —
but case quotes render into `title=` attributes and sources into `href`, so it reported three
false failures. Re-checked against raw HTML: all passed. Same false-negative class as the earlier
Tavily-markdown incident. *Always match against the representation the data actually lives in.*

## 2026-07-20 — Org directory + alignment-research feed

**Seed:** the logo wall of alignmentalignment.ai (CAAAC), a satire site whose "completely
unaffiliated with these AI alignment organizations" wall is a real 31-org list. Credited on the page.

**Discovery:** 7 parallel Opus agents (CAAAC acronyms, technical labs, academic centers, policy,
government+frontier-lab teams+funders, field-building/community, and the harms clusters alignment
orgs don't touch — military, surveillance, CSAM/NCII, labor, provenance, copyright).
Each agent had to *fetch* each site, find a publication feed, and map risk vectors from actual
published output rather than mission statements.

**Result: 389 raw → 321 unique orgs, 59 countries, 52/53 risk vectors covered, 149 RSS feeds.**

### The one design idea

`alignment_orgs` does double duty: it is both the public directory AND the feed registry.
An org row carries its description *and* its `feed_url`, so adding an org makes it appear on the
page and start being ingested. No second list.

### Coverage — the actual finding

| orgs | risk vector |
|---|---|
| 111 | LLM governance is lacking |
| 62 | Evaluations are confounded and biased |
| 47 | Failure of democratic oversight |
| … | |
| 2 | Owner-controlled ideological steering |
| 1 | In-context learning is a black box |
| 1 | Latent data erasure via safety filtering |
| 0 | GAN-based military training |

111 orgs on governance vs 1 on in-context learning. Caveat kept explicit on the page: a thin row
can mean "nobody works on this" OR "our framing is unusual and no org self-describes that way"
(true for *AI denialism*, *latent data erasure*). Those are different claims.

### Org churn the agents caught (a static list would enshrine all of these)

- `eleos.ai` is a **parked domain-for-sale page**; the real org is `eleosai.org`
- **CLR** (Center on Long-Term *Risk*) vs **CLTR** (Centre for Long-Term *Resilience*) — one letter apart, both listed
- US AISI → **NIST CAISI**; UK AI *Safety* Institute → AI *Security* Institute; **GPAI folded into OECD.AI**
- ARC Evals → METR (not double-counted); Pattern Labs → Irregular; PIBBSS → Principles of Intelligence;
  Impact Academy → SteadRise; Encode Justice → Encode; Stiftung Neue Verantwortung → interface;
  Timaeus merging into the newly-launched Resolution
- Robust Intelligence's domain no longer resolves (Cisco); Conjecture fully pivoted commercial;
  Oxford FHI excluded (closed 2024); Stanford Internet Observatory → TIP Center

### Feed: a sibling source into the news ingester, with ZERO edits to its files

`research_pipeline.py` rebinds `pipeline.feeds` and `pipeline.prefilter`, then calls their `run()`.
Everything downstream is shared (dedupe → news_queue → Qwen routing → Opus gate → human apply).
Sources: 149 org RSS feeds + ~22 *targeted* arXiv queries + Alignment Forum/LessWrong.
**Live run: 4,278 raw → 620 after filtering.** Feed health (109 ok / 16 empty / 1 blocked) is
written back to the registry and rendered as a tracked/page-only/no-feed badge — the page is
honest about which orgs we can actually keep current.

### Three bugs found in my own code (all silent-failure class)

1. **`\blanguage model\b` does not match "language models."** The trailing `\b` fails against the
   plural 's'. Since abstracts overwhelmingly use plurals, this silently dropped most real research
   — 78 genuine items recovered once fixed ("Multilingual Safety Alignment Is Not Just Translate
   the Prompt", "Deception in Reinforced Autonomous Agents", …). Every countable term now carries `s?`.
2. **ElementTree rejects a feed with a leading newline before `<?xml`** ("XML or text declaration
   not at start of entity") — losing PAX and other WordPress feeds entirely. Parser now strips
   BOM/whitespace and falls back to seeking the real root element.
3. **`deploy.sh` preflight used `grep -c`** (counts *lines*); the minified 52-page atlas bundle
   reported as 3 and blocked a valid publish. Now counts occurrences.

All three failed *silently* — nothing errored, output was just quietly smaller. Sampling and
asserting on known-good cases is what surfaced them, not reading the code.

### Note for the news-ingester session

`pipeline.py` still defaults `AIPI_KB_ROLE=campus`; ownership was consolidated onto `school`
(campus is superuser, so nothing breaks today). Flagged rather than edited, to avoid stepping on
a live session. Their README asks for "a feed set" of real-URL feeds — the 149 verified org RSS
feeds in `alignment_orgs` are exactly that, available via `research_feeds.org_feeds()`.

### Update — government/funder slice re-ran, 331 orgs final

The government+frontier-lab+funder agent returned a fuller second pass (53 entries), taking the
registry to **331 orgs / 62 countries**. Additional renames it caught:

- **Open Philanthropy → Coefficient Giving** (Nov 2025); openphilanthropy.org 301s
- **International Network of AI Safety Institutes → NAAIMES** (renamed Dec 2025)
- **xAI → SpaceXAI** — SpaceX acquired xAI (incl. Grok and X) 2 Feb 2026; rebrand 6 Jul 2026
- Cohere For AI → Cohere Labs; Zhipu AI → Z.ai; US AISI → NIST CAISI
- Honest negative findings recorded rather than padded: **Kenya, the Netherlands and Italy have no
  AI safety institute** — the listed bodies are AI Act supervisors or network representatives.
  Korea's `aisi.re.kr` refuses connections from outside Korea.

**Known-stale, deliberately NOT auto-fixed:** the atlas carries 67 `xAI` mentions across 11 risks
and zero `SpaceXAI`. Most are historically correct (the "MechaHitler" incident was xAI in July 2025,
and verbatim source quotes say "xAI"), so a find-replace would falsify quotes and rewrite history.
The two cases worth a human decision are the risk *titles* that read as present-tense company
identity: `generative-ncii-csam` ("Grok / xAI") and `model-ideological-steering`
("Owner-Controlled Ideological Steering"). Left for review.

### Ownership migration completed — news_queue moved to `school`

`pipeline.py` defaulted `AIPI_KB_ROLE=campus`. Flipping that string alone would have **broken the
news ingester**: `news_queue` was campus-owned and `school` had *no* privileges on it at all
(`has_table_privilege` returned false for SELECT/INSERT/UPDATE). Checking before editing is what
caught this — the change looked like a one-word fix.

Actual fix, in order:
1. `ALTER TABLE news_queue OWNER TO school;` + same for `news_queue_id_seq`, with `GRANT ALL` back
   to `campus` (which is a superuser anyway, so its running processes are unaffected).
2. Verified as `school`: 304 staged rows intact, and a real INSERT+UPDATE round-trip inside a
   transaction that was rolled back — privilege bits alone aren't proof the write path works.
3. Only then changed `KB_ROLE` default to `school`. `classify_daemon.py`, `opus_review.py`,
   `enrich.py` and `apply.py` all import `pipeline`, so they inherit it — one change covers the
   whole pipeline.
4. Re-ran pipeline's own DB helpers as `school`: queue count, status breakdown, dedupe hash query
   and the published-case-URL query all work.

Scope note: ~30 other tables in `public` remain campus-owned (`lecture_transcripts`,
`student_sessions`, `book_reports`…). Those belong to the campus application, not the Problems
Index, and were deliberately left alone.

Rollback: `docs/news_queue_owner_rollback.txt`.
